const styles = (theme: Object) => ({
  dialogContainer: {
    maxWidth: 760,
    minWidth: 340,
  },
  label: {
    fontSize: 12,
    color: theme.palette.neutrals.mainText,
  },
  content: {
    marginTop: 16,
  },
  mainContent: {
    marginTop: 32,
  },
});

export default styles;
