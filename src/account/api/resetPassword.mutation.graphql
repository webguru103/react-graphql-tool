mutation ResetPassword($password: String!) {
  user: resetUserPassword(password: $password) {
    id
    email
    firstName
    lastName
    phone
    unit
    streetNumber
    streetName
    city
    province
    postalCode
    country
    createdAt
    updatedAt
    loggedInAt
    passwordChangedAt
  }
}
